[project]
name = "property-kb-scripts"
version = "0.1.0"
description = "Utility scripts for the Property Knowledge Assistant prototype"
requires-python = ">=3.12"
dependencies = [
    "azure-identity>=1.25.3",
    "azure-storage-blob>=12.30.2",
    "requests>=2.34.2",
]
