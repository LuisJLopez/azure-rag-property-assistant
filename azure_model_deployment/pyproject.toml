[project]
name = "azure-model-deployment"
version = "0.1.0"
description = "Sanity-check script for a deployed Foundry chat model"
requires-python = ">=3.12"
dependencies = [
    "azure-identity>=1.25.3",
    "openai>=3.15.0",
    "python-dotenv>=1.2.3",
]
